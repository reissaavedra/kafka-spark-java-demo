import org.apache.kafka.clients.producer.ProducerConfig;
import org.apache.kafka.common.serialization.StringSerializer;
//import org.apache.kafka.streams.StreamsBuilder;
//import org.apache.kafka.streams.StreamsConfig;
//import org.apache.kafka.streams.kstream.Consumed;

import java.util.Properties;

public class KTableAggDemo {

//    private Properties streamProps(String bootstrapServer){
//        Properties props = new Properties();
//        props.put(StreamsConfig.APPLICATION_ID_CONFIG, AppConfigs.appID);
//        props.put(StreamsConfig.BOOTSTRAP_SERVERS_CONFIG, AppConfigs.bootstrapServer);
//        props.put(StreamsConfig.STATE_DIR_CONFIG, AppConfigs.stateStoreLocation);
//        props.put(StreamsConfig.COMMIT_INTERVAL_MS_CONFIG,100);
//        return props;
//    }
//
//    StreamsBuilder streamBuilder = new StreamsBuilder();
//    streamBuilder.table(AppConfigs.topicName,Consumed.with(App))

}
